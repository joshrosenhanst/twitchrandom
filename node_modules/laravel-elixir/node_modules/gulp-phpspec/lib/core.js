/*global module */

// TODO: This routine needs to be flushed out

'use strict';

var cmd = '';

var Utils = {
	buildCommand: function(bin, options) {
		cmd = bin;
		return cmd;
	}
}

module.exports = Utils;
